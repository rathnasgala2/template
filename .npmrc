# This repository's own dev/CI install only; a consumer's `npm install` of
# the published package never reads this file (it is not in package.json's
# `files`), so it does not contribute to TPL-H5's EBADENGINE failure mode.
audit=true
engine-strict=true
fund=false
ignore-scripts=true
package-lock=true
save-exact=true
